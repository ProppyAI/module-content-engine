{
  "name": "content-engine",
  "version": "2.0.0",
  "description": "AI-powered content generation via Higgsfield Marketing Studio — images, 3D assets, video, UGC, ads, branded documents",
  "category": "content",
  "entities": {
    "produces": ["content-asset"],
    "consumes": ["client", "job", "estimate"],
    "extends": {}
  },
  "tools": [
    { "name": "generate-image", "description": "Generate a branded image via Higgsfield NanoBanana", "permissions": ["job:read", "client:read", "content-asset:write"] },
    { "name": "generate-video", "description": "Generate a video via Higgsfield SeedDance 2.0", "permissions": ["content-asset:read", "content-asset:write"] },
    { "name": "generate-voiceover", "description": "Generate voice narration via Higgsfield", "permissions": ["content-asset:write"] },
    { "name": "generate-3d-asset", "description": "Generate a 3D model or product render via NanoBanana", "permissions": ["job:read", "content-asset:write"] },
    { "name": "generate-ugc", "description": "Generate UGC-style content — avatar + video via NanoBanana + SeedDance", "permissions": ["client:read", "job:read", "content-asset:write"] },
    { "name": "generate-ad", "description": "Assemble a campaign-ready ad via Marketing Studio", "permissions": ["content-asset:read", "content-asset:write"] },
    { "name": "render-proposal", "description": "Render a branded proposal PDF from an estimate", "permissions": ["estimate:read", "client:read", "content-asset:write"] },
    { "name": "render-invoice", "description": "Render a branded invoice PDF", "permissions": ["client:read", "content-asset:write"] }
  ],
  "hooks": [
    { "event": "PostJobCompleted", "action": "auto-generate-completion-photo", "type": "post" }
  ],
  "dependencies": [],
  "externalServices": [
    { "name": "higgsfield", "required": true, "type": "browser" }
  ],
  "config": {
    "higgsfield_base_url": { "type": "string", "default": "https://studio.higgsfield.ai" },
    "browser_connect_method": { "type": "string", "default": "persistent-context", "enum": ["persistent-context", "cdp"] },
    "browser_data_dir": { "type": "string", "default": ".harness/browser-data" },
    "asset_output_dir": { "type": "string", "default": ".harness/content-assets" },
    "generation_timeout_seconds": { "type": "number", "default": 300 },
    "auto_generate_on_job_complete": { "type": "boolean", "default": true }
  },
  "agents": [
    {
      "name": "content-agent",
      "description": "AI content generation via Higgsfield — images, 3D, video, UGC, ads, documents",
      "capabilities": ["image:generate", "video:generate", "3d:generate", "ugc:generate", "ad:generate", "document:render"]
    }
  ],
  "cron": []
}
